# LSB-steganography
Secret message, a C application that hides the user message in an image using basic Steganography. The ASCII values of lower bytes of the image are changed into the ASCII values of the message so that the changes in image are negligible which is not visible to human eyes.
